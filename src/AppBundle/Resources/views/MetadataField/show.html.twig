{% extends 'base.html.twig' %}

 {% block pageheader %} 
    <h1>MetadataField</h1>
 {% endblock %} 

{% block body %}

    <div class="btn-toolbar">
    <div class="btn-group pull-right">
        <a href="{{ path('metadata_field_index') }}" class="btn btn-default">
            <span class="glyphicon glyphicon-arrow-left"></span> Back
        </a>
        <a href="{{ path('metadata_field_edit', { 'id': metadataField.id }) }}" class="btn btn-default">
            <span class='glyphicon glyphicon-edit'></span> Edit
        </a>
        <a href="{{ path('metadata_field_delete', { 'id': metadataField.id }) }}" class="btn btn-default" data-confirm="Are you sure you want to delete this MetadataField?">
            <span class='glyphicon glyphicon-remove'></span> Delete
        </a>
    </div>
</div>
    <table class="table table-bordered table-condensed table-hover table-striped">
        <tbody>
            <tr>
                <th>Created</th>
                <td>{% if metadataField.created %}{{ metadataField.created|date }}{% endif %}</td>
            </tr>
            <tr>
                <th>Updated</th>
                <td>{% if metadataField.updated %}{{ metadataField.updated|date }}{% endif %}</td>
            </tr>
        </tbody>
    </table>

{% endblock %}
