{% extends 'base.html.twig' %}

{% block body %}
    <h1>Channel Creation</h1>

    <p>At least one of Youtube Id, Title, or URL must be specified.</p>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="form-group">
        <div class="col-sm-2"></div>
        <div class="col-sm-10">
            <input type="submit" value="Create" class="btn btn-primary" />
            <a href="{{ path('channel_index') }}" class="btn btn-warning">Cancel</a>
        </div>
    </div>
    {{ form_end(form) }}

{% endblock %}
